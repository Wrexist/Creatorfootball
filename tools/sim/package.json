{
  "name": "@cf/sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:economy": "tsx src/economyAudit.ts",
    "audit:sim": "tsx src/simAudit.ts",
    "audit:invariants": "tsx src/invariantAudit.ts",
    "audit:all": "tsx src/runAll.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no unit tests; audits run via audit:all\"",
    "experiment:bench": "tsx src/benchExperiment.ts"
  },
  "dependencies": {
    "@cf/engine": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "@types/node": "^22.10.2"
  }
}
